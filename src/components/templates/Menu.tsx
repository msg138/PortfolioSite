import React, { ReactElement } from 'react';
import BackgroundSlash from '../atoms/BackgroundSlash';

const Menu = (): ReactElement => {
  return (
    <div>
      This is menu
    <BackgroundSlash color="red" size="medium" />
    </div>
  );
};

export default Menu;

